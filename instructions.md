Login via npm CLI with preferlist
Run `npm publish --access public` to publish to npm
Run `browserify index.js -o referlist.js` to create `referlist.js` that you store in `referlist/client/public/resources`